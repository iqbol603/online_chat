-- Опциональный индекс для оптимизации запросов статистики по операторам
-- Этот составной индекс ускорит запросы, которые фильтруют по assigned_operator_id, status и closed_at
-- Индекс покрывает все поля, используемые в запросе статистики

-- Проверяем, существует ли индекс, перед добавлением (для MySQL 5.7+)
-- Если индекс уже существует, команда выдаст ошибку, но это не критично

ALTER TABLE `conversations` 
ADD INDEX `idx_operator_status_closed` (`assigned_operator_id`, `status`, `closed_at`);
